import Mediator from '../../../mediators/<%= dasherizedModuleName %>';
import { module, test } from 'qunit';

module('Unit | Mediator | <%= classifiedModuleName %>');

// Replace this with your real tests.
test('it works', function(assert) {
  var subject = Mediator.create();
  assert.ok(subject);
});
