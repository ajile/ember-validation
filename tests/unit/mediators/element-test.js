import Mediator from '../../../mediators/element';
import { module, test } from 'qunit';

module('Unit | Mediator | Element');

// Replace this with your real tests.
test('it works', function(assert) {
  var subject = Mediator.create();
  assert.ok(subject);
});
